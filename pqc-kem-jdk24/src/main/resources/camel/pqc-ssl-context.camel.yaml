# Licensed to the Apache Software Foundation (ASF) under one or more
# contributor license agreements.  See the NOTICE file distributed with
# this work for additional information regarding copyright ownership.
# The ASF licenses this file to You under the Apache License, Version 2.0
# (the "License"); you may not use this file except in compliance with
# the License.  You may obtain a copy of the License at
#
#      http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Post-Quantum TLS Handshake Example using BouncyCastle JSSE on JDK 24
#
# Demonstrates a real PQC TLS 1.3 handshake with X25519MLKEM768 hybrid
# key exchange using BouncyCastle's JSSE provider (BCJSSE), without
# requiring JDK 27's JEP 527.
#
# The self-contained test creates an SSLServerSocket and SSLSocket,
# both using BCJSSE, with the client offering X25519MLKEM768 for
# key exchange and secp256r1 for ECDSA authentication.
# A successful handshake proves PQC key exchange was negotiated.

# Route 1: PQC TLS handshake verification at startup
# Generates EC key pair via BC, creates SSLContext via BCJSSE,
# performs a real TLS 1.3 handshake with X25519MLKEM768.
- route:
    id: pqc-verify
    from:
      uri: timer:pqc-verify
      parameters:
        repeatCount: 1
        delay: 1000
      steps:
        - log: "=== Post-Quantum TLS Handshake Verification (BouncyCastle JSSE on JDK 24) ==="
        - script:
            groovy: |
              import javax.net.ssl.*
              import java.security.*
              import java.security.cert.X509Certificate
              import java.math.BigInteger
              import java.util.Date
              import org.bouncycastle.asn1.x500.X500Name
              import org.bouncycastle.cert.jcajce.JcaX509CertificateConverter
              import org.bouncycastle.cert.jcajce.JcaX509v3CertificateBuilder
              import org.bouncycastle.operator.jcajce.JcaContentSignerBuilder

              def serverSocket = null
              def accepted = null

              try {
                  def bcProvider = Security.getProvider("BC")

                  // Generate EC key pair using BouncyCastle with explicit named curve
                  def kpg = KeyPairGenerator.getInstance("EC", bcProvider)
                  kpg.initialize(new java.security.spec.ECGenParameterSpec("secp256r1"))
                  def keyPair = kpg.generateKeyPair()

                  // Create self-signed certificate using BouncyCastle
                  def subject = new X500Name("CN=localhost, OU=Camel, O=Apache, C=US")
                  def now = new Date()
                  def notBefore = new Date(now.getTime() - 86400000L)
                  def notAfter = new Date(now.getTime() + 365L * 86400000L)
                  def serial = BigInteger.valueOf(System.currentTimeMillis())

                  def certBuilder = new JcaX509v3CertificateBuilder(
                      subject, serial, notBefore, notAfter, subject, keyPair.getPublic())

                  def signer = new JcaContentSignerBuilder("SHA256withECDSA")
                      .setProvider(bcProvider).build(keyPair.getPrivate())
                  def cert = new JcaX509CertificateConverter()
                      .setProvider(bcProvider).getCertificate(certBuilder.build(signer))

                  // Create in-memory JKS keystore with the BC-generated key
                  def ks = KeyStore.getInstance("JKS")
                  ks.load(null, null)
                  ks.setKeyEntry("server", keyPair.getPrivate(),
                      "changeit".toCharArray(),
                      [cert] as java.security.cert.Certificate[])

                  // Create truststore with the same certificate
                  def ts = KeyStore.getInstance("JKS")
                  ts.load(null, null)
                  ts.setCertificateEntry("server", cert)

                  // Use BCJSSE's PKIX KeyManagerFactory and TrustManagerFactory
                  def kmf = KeyManagerFactory.getInstance("PKIX", "BCJSSE")
                  kmf.init(ks, "changeit".toCharArray())

                  def tmf = TrustManagerFactory.getInstance("PKIX", "BCJSSE")
                  tmf.init(ts)

                  // Create SSLContext using BCJSSE
                  def sslCtx = SSLContext.getInstance("TLSv1.3", "BCJSSE")
                  sslCtx.init(kmf.getKeyManagers(), tmf.getTrustManagers(),
                      SecureRandom.getInstance("DEFAULT", bcProvider))

                  // --- Server side ---
                  serverSocket = sslCtx.getServerSocketFactory().createServerSocket(0) as SSLServerSocket
                  def port = serverSocket.getLocalPort()

                  // --- Client side (in a thread) ---
                  def handshakeResult = new java.util.concurrent.CompletableFuture()

                  Thread.start {
                      try {
                          def cs = sslCtx.getSocketFactory().createSocket("localhost", port)

                          // Client offers X25519MLKEM768 for PQC key exchange
                          // and secp256r1 for ECDSA signature verification.
                          // Both are needed: X25519MLKEM768 for the key exchange,
                          // secp256r1 because the server uses an EC certificate.
                          def bcClientParams = new org.bouncycastle.jsse.BCSSLParameters()
                          bcClientParams.setNamedGroups(["X25519MLKEM768", "secp256r1"] as String[])
                          ((org.bouncycastle.jsse.BCSSLSocket) cs).setParameters(bcClientParams)

                          cs.startHandshake()

                          def session = cs.getSession()
                          handshakeResult.complete([
                              protocol: session.getProtocol(),
                              cipherSuite: session.getCipherSuite(),
                              peerHost: session.getPeerHost(),
                          ])
                          cs.close()
                      } catch (Exception e) {
                          handshakeResult.completeExceptionally(e)
                      }
                  }

                  // Server accepts the connection
                  accepted = serverSocket.accept()
                  ((SSLSocket) accepted).startHandshake()
                  def serverSession = ((SSLSocket) accepted).getSession()

                  // Wait for client result
                  def clientResult = handshakeResult.get(10, java.util.concurrent.TimeUnit.SECONDS)

                  def result = [
                      pqcVerified: true,
                      tlsProvider: "BouncyCastle JSSE (BCJSSE)",
                      jdkVersion: System.getProperty("java.version"),
                      server: [
                          protocol: serverSession.getProtocol(),
                          cipherSuite: serverSession.getCipherSuite(),
                      ],
                      client: [
                          protocol: clientResult.protocol,
                          cipherSuite: clientResult.cipherSuite,
                          namedGroupsOffered: "X25519MLKEM768 (PQC key exchange) + secp256r1 (ECDSA auth)",
                      ],
                      message: "PQC VERIFIED: TLS 1.3 handshake with X25519MLKEM768 succeeded on JDK ${System.getProperty('java.version')} using BouncyCastle JSSE"
                  ]

                  exchange.message.setBody(groovy.json.JsonOutput.prettyPrint(
                      groovy.json.JsonOutput.toJson(result)))

              } catch (Exception e) {
                  def result = [
                      pqcVerified: false,
                      tlsProvider: "BouncyCastle JSSE (BCJSSE)",
                      jdkVersion: System.getProperty("java.version"),
                      error: e.getClass().getSimpleName() + ": " + e.getMessage(),
                      message: "PQC TLS handshake FAILED"
                  ]
                  if (e.getCause() != null) {
                      result.cause = e.getCause().getClass().getSimpleName() + ": " + e.getCause().getMessage()
                  }
                  exchange.message.setBody(groovy.json.JsonOutput.prettyPrint(
                      groovy.json.JsonOutput.toJson(result)))
              } finally {
                  if (accepted != null) try { accepted.close() } catch (Exception ignore) {}
                  if (serverSocket != null) try { serverSocket.close() } catch (Exception ignore) {}
              }
        - log: "PQC Verification Result:\n${body}"

# Route 2: REST API endpoint (served over plain HTTP)
- route:
    id: pqc-secure-server
    from:
      uri: platform-http:/api/data
      parameters:
        httpMethodRestrict: GET
      steps:
        - log: "Received request on /api/data"
        - script:
            groovy: |
              exchange.message.setBody(groovy.json.JsonOutput.prettyPrint(
                  groovy.json.JsonOutput.toJson([
                      message: "PQC TLS handshake verified at startup using BouncyCastle JSSE on JDK " + System.getProperty("java.version"),
                      algorithm: "X25519MLKEM768 (hybrid: X25519 + ML-KEM-768)",
                      protocol: "TLS 1.3",
                      timestamp: new Date().format("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
                  ])))
        - setHeader:
            name: Content-Type
            constant: "application/json"

# Route 3: REST endpoint to run PQC verification on demand
- route:
    id: pqc-verify-endpoint
    from:
      uri: platform-http:/api/verify-pqc
      parameters:
        httpMethodRestrict: GET
      steps:
        - log: "PQC verification requested via API"
        - script:
            groovy: |
              import javax.net.ssl.*
              import java.security.*
              import java.security.cert.X509Certificate
              import java.math.BigInteger
              import java.util.Date
              import org.bouncycastle.asn1.x500.X500Name
              import org.bouncycastle.cert.jcajce.JcaX509v3CertificateBuilder
              import org.bouncycastle.cert.jcajce.JcaX509CertificateConverter
              import org.bouncycastle.operator.jcajce.JcaContentSignerBuilder

              def serverSocket = null
              def accepted = null

              try {
                  def bcProvider = Security.getProvider("BC")
                  def kpg = KeyPairGenerator.getInstance("EC", bcProvider)
                  kpg.initialize(new java.security.spec.ECGenParameterSpec("secp256r1"))
                  def keyPair = kpg.generateKeyPair()

                  def subject = new X500Name("CN=localhost, O=Apache, C=US")
                  def now = new Date()
                  def serial = BigInteger.valueOf(System.currentTimeMillis())
                  def certBuilder = new JcaX509v3CertificateBuilder(
                      subject, serial,
                      new Date(now.getTime() - 86400000L),
                      new Date(now.getTime() + 365L * 86400000L),
                      subject, keyPair.getPublic())
                  def signer = new JcaContentSignerBuilder("SHA256withECDSA")
                      .setProvider(bcProvider).build(keyPair.getPrivate())
                  def cert = new JcaX509CertificateConverter()
                      .setProvider(bcProvider).getCertificate(certBuilder.build(signer))

                  def ks = KeyStore.getInstance("JKS")
                  ks.load(null, null)
                  ks.setKeyEntry("server", keyPair.getPrivate(),
                      "changeit".toCharArray(),
                      [cert] as java.security.cert.Certificate[])

                  def ts = KeyStore.getInstance("JKS")
                  ts.load(null, null)
                  ts.setCertificateEntry("server", cert)

                  def kmf = KeyManagerFactory.getInstance("PKIX", "BCJSSE")
                  kmf.init(ks, "changeit".toCharArray())
                  def tmf = TrustManagerFactory.getInstance("PKIX", "BCJSSE")
                  tmf.init(ts)

                  def sslCtx = SSLContext.getInstance("TLSv1.3", "BCJSSE")
                  sslCtx.init(kmf.getKeyManagers(), tmf.getTrustManagers(),
                      SecureRandom.getInstance("DEFAULT", bcProvider))

                  serverSocket = sslCtx.getServerSocketFactory().createServerSocket(0) as SSLServerSocket
                  def port = serverSocket.getLocalPort()

                  def handshakeResult = new java.util.concurrent.CompletableFuture()

                  Thread.start {
                      try {
                          def cs = sslCtx.getSocketFactory().createSocket("localhost", port)
                          def bcClientParams = new org.bouncycastle.jsse.BCSSLParameters()
                          bcClientParams.setNamedGroups(["X25519MLKEM768", "secp256r1"] as String[])
                          ((org.bouncycastle.jsse.BCSSLSocket) cs).setParameters(bcClientParams)
                          cs.startHandshake()
                          def s = cs.getSession()
                          handshakeResult.complete([protocol: s.getProtocol(), cipherSuite: s.getCipherSuite()])
                          cs.close()
                      } catch (Exception e) {
                          handshakeResult.completeExceptionally(e)
                      }
                  }

                  accepted = serverSocket.accept()
                  ((SSLSocket) accepted).startHandshake()

                  def clientResult = handshakeResult.get(10, java.util.concurrent.TimeUnit.SECONDS)

                  exchange.message.setBody(groovy.json.JsonOutput.prettyPrint(
                      groovy.json.JsonOutput.toJson([
                          pqcVerified: true,
                          tlsProvider: "BouncyCastle JSSE (BCJSSE)",
                          jdkVersion: System.getProperty("java.version"),
                          protocol: clientResult.protocol,
                          cipherSuite: clientResult.cipherSuite,
                          namedGroup: "X25519MLKEM768",
                          message: "PQC VERIFIED: TLS handshake succeeded with X25519MLKEM768 key exchange"
                      ])))
              } catch (Exception e) {
                  def msg = e.getMessage() ?: e.getCause()?.getMessage() ?: "unknown error"
                  exchange.message.setBody(groovy.json.JsonOutput.prettyPrint(
                      groovy.json.JsonOutput.toJson([
                          pqcVerified: false,
                          error: msg,
                          message: "PQC verification FAILED"
                      ])))
              } finally {
                  if (accepted != null) try { accepted.close() } catch (Exception ignore) {}
                  if (serverSocket != null) try { serverSocket.close() } catch (Exception ignore) {}
              }
        - setHeader:
            name: Content-Type
            constant: "application/json"

# Route 4: ML-KEM Key Encapsulation using JDK 24 native KEM API
# This route demonstrates JDK 24's javax.crypto.KEM with ML-KEM (JEP 496),
# running encapsulate/decapsulate with both JDK native and BouncyCastle
# providers, plus cross-provider interoperability tests.
- route:
    id: ml-kem-verify
    from:
      uri: platform-http:/api/verify-kem
      parameters:
        httpMethodRestrict: GET
      steps:
        - log: "ML-KEM verification requested via API"
        - script:
            groovy: |
              import javax.crypto.KEM
              import java.security.*
              import java.security.spec.NamedParameterSpec
              import java.security.spec.X509EncodedKeySpec
              import java.security.spec.PKCS8EncodedKeySpec

              try {
                  def results = []

                  // --- Test 1: JDK native ML-KEM-768 ---
                  def jdkKpg = KeyPairGenerator.getInstance("ML-KEM")
                  jdkKpg.initialize(NamedParameterSpec.ML_KEM_768)
                  def jdkKp = jdkKpg.generateKeyPair()

                  def jdkKem = KEM.getInstance("ML-KEM")
                  def jdkEnc = jdkKem.newEncapsulator(jdkKp.getPublic())
                  def jdkEncap = jdkEnc.encapsulate()

                  def jdkDec = jdkKem.newDecapsulator(jdkKp.getPrivate())
                  def jdkRecovered = jdkDec.decapsulate(jdkEncap.encapsulation())

                  def jdkMatch = Arrays.equals(
                      jdkEncap.key().getEncoded(),
                      jdkRecovered.getEncoded())

                  results.add([
                      test: "JDK native -> JDK native",
                      provider: jdkKpg.getProvider().getName(),
                      algorithm: "ML-KEM-768",
                      publicKeySize: jdkKp.getPublic().getEncoded().length,
                      ciphertextSize: jdkEncap.encapsulation().length,
                      sharedSecretSize: jdkEncap.key().getEncoded().length,
                      secretsMatch: jdkMatch,
                  ])

                  // --- Test 2: BouncyCastle ML-KEM-768 ---
                  def bcKpg = KeyPairGenerator.getInstance("ML-KEM", "BC")
                  bcKpg.initialize(
                      org.bouncycastle.jcajce.spec.MLKEMParameterSpec.ml_kem_768)
                  def bcKp = bcKpg.generateKeyPair()

                  def bcKem = KEM.getInstance("ML-KEM", "BC")
                  def bcEnc = bcKem.newEncapsulator(bcKp.getPublic())
                  def bcEncap = bcEnc.encapsulate()

                  def bcDec = bcKem.newDecapsulator(bcKp.getPrivate())
                  def bcRecovered = bcDec.decapsulate(bcEncap.encapsulation())

                  def bcMatch = Arrays.equals(
                      bcEncap.key().getEncoded(),
                      bcRecovered.getEncoded())

                  results.add([
                      test: "BouncyCastle -> BouncyCastle",
                      provider: bcKpg.getProvider().getName(),
                      algorithm: "ML-KEM-768",
                      publicKeySize: bcKp.getPublic().getEncoded().length,
                      ciphertextSize: bcEncap.encapsulation().length,
                      sharedSecretSize: bcEncap.key().getEncoded().length,
                      secretsMatch: bcMatch,
                  ])

                  // --- Test 3: Cross-provider (JDK keys re-encoded for BC KEM) ---
                  // Providers don't recognize each other's key objects directly,
                  // so we re-encode through standard formats (X.509 / PKCS#8).
                  def crossTest1 = [test: "JDK keygen -> BC encapsulate/decapsulate (re-encoded)"]
                  try {
                      def bcKf = KeyFactory.getInstance("ML-KEM", "BC")
                      def reEncodedPub = bcKf.generatePublic(
                          new X509EncodedKeySpec(jdkKp.getPublic().getEncoded()))
                      def reEncodedPriv = bcKf.generatePrivate(
                          new PKCS8EncodedKeySpec(jdkKp.getPrivate().getEncoded()))

                      def crossKem = KEM.getInstance("ML-KEM", "BC")
                      def crossEnc = crossKem.newEncapsulator(reEncodedPub)
                      def crossEncap = crossEnc.encapsulate()
                      def crossDec = crossKem.newDecapsulator(reEncodedPriv)
                      def crossRecovered = crossDec.decapsulate(crossEncap.encapsulation())
                      crossTest1.secretsMatch = Arrays.equals(
                          crossEncap.key().getEncoded(),
                          crossRecovered.getEncoded())
                  } catch (Exception e) {
                      crossTest1.secretsMatch = false
                      crossTest1.error = e.getClass().getSimpleName() + ": " + e.getMessage()
                  }
                  results.add(crossTest1)

                  // --- Test 4: Cross-provider (BC keys re-encoded for JDK KEM) ---
                  def crossTest2 = [test: "BC keygen -> JDK encapsulate/decapsulate (re-encoded)"]
                  try {
                      def jdkKf = KeyFactory.getInstance("ML-KEM")
                      def reEncodedPub2 = jdkKf.generatePublic(
                          new X509EncodedKeySpec(bcKp.getPublic().getEncoded()))
                      def reEncodedPriv2 = jdkKf.generatePrivate(
                          new PKCS8EncodedKeySpec(bcKp.getPrivate().getEncoded()))

                      def crossKem2 = KEM.getInstance("ML-KEM")
                      def crossEnc2 = crossKem2.newEncapsulator(reEncodedPub2)
                      def crossEncap2 = crossEnc2.encapsulate()
                      def crossDec2 = crossKem2.newDecapsulator(reEncodedPriv2)
                      def crossRecovered2 = crossDec2.decapsulate(crossEncap2.encapsulation())
                      crossTest2.secretsMatch = Arrays.equals(
                          crossEncap2.key().getEncoded(),
                          crossRecovered2.getEncoded())
                  } catch (Exception e) {
                      crossTest2.secretsMatch = false
                      crossTest2.error = e.getClass().getSimpleName() + ": " + e.getMessage()
                  }
                  results.add(crossTest2)

                  def allPassed = results.every { it.secretsMatch == true }

                  exchange.message.setBody(groovy.json.JsonOutput.prettyPrint(
                      groovy.json.JsonOutput.toJson([
                          kemVerified: allPassed,
                          jdkVersion: System.getProperty("java.version"),
                          algorithm: "ML-KEM-768 (FIPS 203)",
                          tests: results,
                          message: allPassed
                              ? "ALL TESTS PASSED: ML-KEM works with both JDK native and BouncyCastle providers"
                              : "SOME TESTS FAILED: Check individual test results"
                      ])))

              } catch (Exception e) {
                  def msg = e.getMessage() ?: e.getCause()?.getMessage() ?: "unknown error"
                  exchange.message.setBody(groovy.json.JsonOutput.prettyPrint(
                      groovy.json.JsonOutput.toJson([
                          kemVerified: false,
                          error: msg,
                          message: "ML-KEM verification FAILED"
                      ])))
              }
        - setHeader:
            name: Content-Type
            constant: "application/json"

# Route 5: SSL configuration info endpoint
- route:
    id: pqc-ssl-info
    from:
      uri: platform-http:/api/ssl-info
      parameters:
        httpMethodRestrict: GET
      steps:
        - script:
            groovy: |
              import javax.net.ssl.*
              import java.security.*

              def providers = Security.getProviders().collect { it.getName() + " " + it.getVersionStr() }

              def bcjsseAvailable = Security.getProvider("BCJSSE") != null
              def bcAvailable = Security.getProvider("BC") != null

              def pqcGroups = []
              try {
                  def namedGroupClass = Class.forName("org.bouncycastle.tls.NamedGroup")
                  def pqcCodePoints = [
                      [0x11EC, "X25519MLKEM768"],
                      [0x11EB, "SecP256r1MLKEM768"],
                      [0x11ED, "SecP384r1MLKEM1024"],
                      [0x0200, "MLKEM512"],
                      [0x0201, "MLKEM768"],
                      [0x0202, "MLKEM1024"],
                  ]
                  pqcCodePoints.each { cp ->
                      try {
                          def name = namedGroupClass.getMethod("getName", int.class).invoke(null, cp[0])
                          if (name != null) { pqcGroups.add(name) }
                      } catch (Exception ignore) {}
                  }
              } catch (Exception ignore) {}

              // Check for JDK 24 native KEM support
              def nativeKemAlgorithms = []
              try {
                  Security.getProviders().each { p ->
                      p.getServices().each { s ->
                          if (s.getType() == "KEM" && s.getAlgorithm().startsWith("ML-KEM")) {
                              nativeKemAlgorithms.add(p.getName() + ": " + s.getAlgorithm())
                          }
                      }
                  }
              } catch (Exception ignore) {}

              def enabledCiphers = []
              def enabledProtocols = []
              try {
                  def ctx = SSLContext.getInstance("TLSv1.3", "BCJSSE")
                  ctx.init(null, null, null)
                  def params = ctx.getDefaultSSLParameters()
                  enabledCiphers = params.getCipherSuites()?.toList() ?: []
                  enabledProtocols = params.getProtocols()?.toList() ?: []
              } catch (Exception ignore) {}

              exchange.message.setBody(groovy.json.JsonOutput.prettyPrint(
                  groovy.json.JsonOutput.toJson([
                      javaVersion: System.getProperty("java.version"),
                      javaVendor: System.getProperty("java.vendor"),
                      bcjsseProvider: bcjsseAvailable,
                      bcProvider: bcAvailable,
                      pqcNamedGroups: pqcGroups,
                      nativeKemSupport: nativeKemAlgorithms,
                      enabledProtocols: enabledProtocols,
                      enabledCipherSuites: enabledCiphers,
                      securityProviders: providers
                  ])))
        - setHeader:
            name: Content-Type
            constant: "application/json"
